server:
    port: "8888" # type: string
    jwt:
        expires: 48 # hours
        issuer: "string"
        secret: "string"

database:
    username: "username"
    password: "password"
    address: "address:port"
    dbname: "databaseName"

secret: "something"

email:
    sender: "example@example.com"
    pwd: "your email token"
    smtp_addr: "smtp.qq.com"
    smtp_port: 25
    receiver: "another email"

env: "dev"  # 环境 dev for 开发环境; pro for 生产环境